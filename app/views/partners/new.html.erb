<div class="container">
	<div class="row">
		<div class="col-md-8 col-md-offset-2">
			<%= simple_form_for @partner do|f| %>
				<%= f.input :name %>
				
				<%= f.input :photo, as: :attachinary %>
				<%= f.button :submit %>
			<% end %>
		</div>
	</div>
</div>