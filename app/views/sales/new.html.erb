<%= simple_form_for @sale do|f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :price %>
	<%= f.input :city %>
	<%= f.input :photos, as: :attachinary %>
	<%= f.button :submit %>
<% end %>